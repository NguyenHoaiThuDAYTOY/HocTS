// KDL Nguyên thủy
let count2: number = 1;
// count1 = 'name';//error
let boolean: boolean = count2 > 0;
let string: string = `${count2} > 0 is ${boolean}`;
console.log('checking count: ', count2)

// KDL Tham chiếu
let names2: string[] = ['nguyen', 'hoai', 'thu']; //array chỉ bao gồm string
let names2Number: number[] = [1, 2, 3]; //array chỉ bao gồm number
// names2.push(25); // error
console.log('checking names: ', names2)
